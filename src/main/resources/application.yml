spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  thymeleaf:
    cache: false

server:
  port: 8080

logging:
  level:
    com.salairemorsang: DEBUG
    org.apache.pdfbox: INFO 